@{
    var meetingId = (long)ViewBag.MeetingId;
}

<div class="col-xs-12 col-md-12 col-lg-12">
    <div class="row">
        <form id="showViewerGridParent" class="app__grid-wrapper">
            @{ Html.RenderAction("FillShowViewerGrid", "MeetingCategoryReport", new { meetingId = meetingId });}
        </form>
    </div>
</div>